# ---------------------------------------------------------------------------
# dev environment  ->  mvn test -Denv=dev   (or -Pdev)
#
# The credentials below are the published sign-in details of two public demo
# services. They are documentation, not secrets: nothing here grants access to
# anything private. Real credentials are injected as environment variables —
# see api.token in config.properties.
# ---------------------------------------------------------------------------

# --- web under test: https://www.saucedemo.com ------------------------------
web.base.url=https://www.saucedemo.com

# --- api under test: https://dummyjson.com ----------------------------------
api.base.uri=https://dummyjson.com
api.username=emilys
api.password=emilyspass
