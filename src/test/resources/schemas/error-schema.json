{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Error response",
  "description": "Errors are part of the contract too: a client that cannot rely on the shape of a failure cannot handle it.",
  "type": "object",
  "required": ["message"],
  "properties": {
    "message": {
      "type": "string",
      "minLength": 1
    }
  }
}
